import { Display, Image, Text } from "@geist-ui/react";
import React from "react";
import Spotify from "../components/Spotify";

const Companies = () => {
  return (
    <div style={{ textAlign: "center" }}>
      <Spotify />
    </div>
  );
};

export default Companies;
